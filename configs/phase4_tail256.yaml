model:
  name: bert-base-uncased
  num_labels: 2

data:
  dataset_name: imdb
  max_length: 256
  truncation_strategy: tail
  validation_size: 0.1
  train_samples: null
  validation_samples: null
  test_samples: null

training:
  seed: 42
  batch_size: 16
  epochs: 3
  learning_rate: 2.0e-5
  weight_decay: 0.01
  warmup_ratio: 0.1
  num_workers: 0
  best_metric: f1

output:
  output_dir: outputs/phase4/tail256/train
  best_model_dir: models/phase4_tail256
